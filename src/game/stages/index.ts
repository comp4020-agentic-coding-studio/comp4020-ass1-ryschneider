import { stage01Triangle } from "./stage-01-triangle";
import type { StageDefinition } from "../stage";

/** Every playable stage, in order. Add a new stage by importing it and appending it here. */
export const STAGES: readonly StageDefinition[] = [stage01Triangle];
